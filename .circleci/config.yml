version: 2
jobs:
  build:
    docker:
      - image: circleci/php:7.2-node-browsers
    steps:
      - checkout
      - run:
          name: "Check Docker"
          command: echo "Hi"
      - run:
          name: "Check Composer"
          command: composer --version
      - run:
          name: "Check Node"
          command: node -v && npm -v
